language: none

services:
   - elasticsearch 


build:
  pre_ci_boot:
    image_name: drydock/c7all
    image_tag: master
    pull: true
    
    
  ci:
    - elasticsearch --version
    - gosu --version
