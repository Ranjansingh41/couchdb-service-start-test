language: none

services:
   - cassandra 
   
build:
  pre_ci:
    - shippable_jdk set openjdk8

build:
  pre_ci_boot:
    image_name: drydock/u16javall
    image_tag: master
    pull: true
    
    
  ci:
     - java -version
